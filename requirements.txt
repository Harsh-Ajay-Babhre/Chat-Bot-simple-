## use this command to install all the libraries needed - 
# ---------------------------------------------------> pip install -r requirements.txt <----------------------------------------------------------#

pip install pyttsx3
pip install speechrecognition
pip install pyaudio
pip install tkinter
pip install requests
pip install beautifulsoup4
pip install transformers
pip install flask
pip install nltk
